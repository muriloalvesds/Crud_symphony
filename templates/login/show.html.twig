{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    <h1>Login</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ login.id }}</td>
            </tr>
            <tr>
                <th>Usuario</th>
                <td>{{ login.usuario }}</td>
            </tr>
            <tr>
                <th>Senha</th>
                <td>{{ login.senha }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_login_index') }}">Voltar a listagem</a>

    <a href="{{ path('app_login_edit', {'id': login.id}) }}">edit</a>

    {{ include('login/_delete_form.html.twig') }}
{% endblock %}
